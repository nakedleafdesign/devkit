$( document ).ready(function( $ ) {
  $('.main-slider').sliderPro({
    width:'100%',
    arrows: false,
    buttons: false,
    slideDistance:0,
    visibleSize: '100%',
    autoHeight:true,
    // autoplay:true,
    autoplay:false,
    autoplayDelay:2000
  });
});